server {
    listen 8080;
    host localhost;
    error_page 404 error_pages/404.html;

    client_max_body_size 100m; 

	root ./;
    location / {
        methods GET;
        root ./;
		autoindex on;
    }

    location .bla {
        methods POST;
        cgi_pass .bla /home/zbengued/Vogsphere/fixed_webserv/intra_test/cgi_tester; 
    }

    location /post_body {
        methods POST;
		client_max_body_size 100m; 
    }

    location /directory {
        methods GET POST;
        root ./YoupiBanane; 
        index youpi.bad_extension;
    }
}
