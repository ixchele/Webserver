#include "Utils.hpp"
#include <Server.hpp>

Socket::Socket(int domain, int type, int protocol) {
	// if ((this->socketFd = socket(domain, type, protocol)) == -1)
		// throw SocketException() << "ca"; 

}

// void	bind
